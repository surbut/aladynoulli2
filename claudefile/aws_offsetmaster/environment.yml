name: aladyn_aws
channels:
  - pytorch
  - conda-forge
  - defaults
dependencies:
  - python=3.9
  - numpy>=1.20.0
  - scipy>=1.7.0
  - pandas>=1.3.0
  - matplotlib>=3.4.0
  - seaborn>=0.11.0
  - scikit-learn>=1.0.0
  - statsmodels>=0.13.0
  - pytorch::pytorch>=1.10.0  # CPU version
  - pip
  - pip:
    # Add any packages that are easier to install via pip
    # Most packages above are available via conda

